<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xianyu.dao.FavoriteMapper">

    <resultMap id="FavoriteMap" type="com.xianyu.entity.Favorite">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="itemId" column="item_id"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <resultMap id="FavoriteWithItemMap" type="com.xianyu.entity.Favorite">
        <id property="id" column="fav_id"/>
        <result property="userId" column="user_id"/>
        <result property="itemId" column="item_id"/>
        <result property="createTime" column="fav_create_time"/>
        <association property="item" javaType="com.xianyu.entity.Item">
            <id property="id" column="item_id"/>
            <result property="title" column="title"/>
            <result property="description" column="description"/>
            <result property="price" column="price"/>
            <result property="originalPrice" column="original_price"/>
            <result property="category" column="category"/>
            <result property="conditions" column="conditions"/>
            <result property="status" column="status"/>
            <result property="sellerId" column="seller_id"/>
            <result property="contactWay" column="contact_way"/>
            <result property="location" column="location"/>
            <result property="imageUrls" column="image_urls"/>
            <result property="viewCount" column="view_count"/>
            <result property="likeCount" column="like_count"/>
            <result property="createTime" column="item_create_time"/>
            <result property="updateTime" column="update_time"/>
        </association>
    </resultMap>

    <insert id="insert" parameterType="com.xianyu.entity.Favorite" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO favorite (user_id, item_id)
        VALUES (#{userId}, #{itemId})
    </insert>

    <delete id="delete">
        DELETE FROM favorite WHERE user_id = #{userId} AND item_id = #{itemId}
    </delete>

    <select id="find" resultMap="FavoriteMap">
        SELECT * FROM favorite WHERE user_id = #{userId} AND item_id = #{itemId}
    </select>

    <select id="listByUser" resultMap="FavoriteWithItemMap">
        SELECT 
            f.id as fav_id,
            f.user_id,
            f.item_id,
            f.create_time as fav_create_time,
            i.title,
            i.description,
            i.price,
            i.original_price,
            i.category,
            i.conditions,
            i.status,
            i.seller_id,
            i.contact_way,
            i.location,
            i.image_urls,
            i.view_count,
            i.like_count,
            i.create_time as item_create_time,
            i.update_time
        FROM favorite f
        LEFT JOIN item i ON f.item_id = i.id
        WHERE f.user_id = #{userId}
        ORDER BY f.create_time DESC
    </select>
</mapper>

