<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xianyu.dao.UserMapper">

    <resultMap id="UserMap" type="com.xianyu.entity.User">
        <id property="id" column="id"/>
        <result property="studentId" column="student_id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="phone" column="phone"/>
        <result property="email" column="email"/>
        <result property="status" column="status"/>
        <result property="role" column="role"/>
    </resultMap>

    <insert id="insert" parameterType="com.xianyu.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (student_id, username, password, phone, email,  status, role)
        VALUES (#{studentId}, #{username}, #{password}, #{phone}, #{email},  #{status}, #{role})
    </insert>

    <select id="findById" resultMap="UserMap">
        SELECT * FROM users WHERE id = #{id}
    </select>

    <select id="findByUsername" resultMap="UserMap">
        SELECT * FROM users WHERE username = #{username}
    </select>

    <select id="findByStudentId" resultMap="UserMap">
        SELECT * FROM users WHERE student_id = #{studentId}
    </select>

    <select id="findAll" resultMap="UserMap">
        SELECT * FROM users ORDER BY id DESC
    </select>

    <update id="updateStatus">
        UPDATE users
        SET status = #{status}
        WHERE id = #{id}
    </update>
</mapper>

